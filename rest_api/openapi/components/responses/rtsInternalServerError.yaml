description: Internal Server Error.
content:
  application/json:
    schema:
      $ref: ../schemas/rtsError.yaml
    examples:
      internal-error:
        $ref: ../examples/rtsExampleInternalServerError.yaml
