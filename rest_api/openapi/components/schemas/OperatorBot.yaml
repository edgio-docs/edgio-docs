title: Operator Object
type: object
properties:
  type:
    type: string
    enum:
      - RX
      - STREQ
      - EQ
      - CONTAINS
      - BEGINSWITH
      - ENDSWITH
      - IPMATCH
  value:
    type: string
  values:
    description: Max 20 entries.
    type: array
    items:
      type: string
  is_negated:
    type: boolean
oneOf:
  - required:
      - value
  - required:
      - values
required:
  - type
