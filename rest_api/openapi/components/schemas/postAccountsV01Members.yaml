required:
  - organization_id
  - user_email
type: object
properties:
  user_email:
    type: string
  organization_id:
    type: string
  role:
    type: string
    enum:
      - read_only
      - purger
      - member
      - admin
      - super_admin
description: Create a new member
