post:
  tags:
    - bff
  operationId: postBffTrafficQueryName
  parameters:
    - name: query_name
      in: path
      required: true
      schema:
        type: string
        enum:
          - paths
          - origins-paths
          - origins-countries
          - origins-overtime
  responses:
    '201':
      description: created Traffic
      content: {}
