post:
  tags:
    - bff
  operationId: postBffTrafficQueryName
  parameters:
    - name: query_name
      in: path
      required: true
      schema:
        type: string
        enum:
          - origins-countries
          - origins-overtime
          - data-usage-overtime
          - routes
  responses:
    '201':
      description: created Traffic
      content: {}
