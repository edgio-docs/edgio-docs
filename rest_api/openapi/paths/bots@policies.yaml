get:
  operationId: get-bots-policies
  tags:
    - Bot Policies
  summary: GET Bot Policies
  description: GET list of policies you can apply in bot rule directive.
  responses:
    '200':
      description: List of policies
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../components/schemas/IncludePolicies.yaml
