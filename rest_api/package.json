{
  "name": "waf-api",
  "version": "1.0.0",
  "dependencies": {
    "@redocly/openapi-cli": "^1.0.0-beta.95"
  },
  "private": true,
  "scripts": {
    "start": "openapi preview-docs",
    "build": "openapi bundle -o dist",
    "test": "openapi lint",
    "pull-console-api": "mkdir -p tmp && ruby scripts/pull_console_api.rb",
    "split-console-api": "mkdir -p tmp/split && openapi split tmp/console_oapi3.json --outDir tmp/split"
  }
}
