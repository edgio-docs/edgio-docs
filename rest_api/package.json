{
  "name": "rest-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "openapi preview-docs",
    "build": "openapi bundle -o dist",
    "test": "openapi lint",
    "console-api:pull": "mkdir -p tmp && sh scripts/pull_console_swagger.sh",
    "console-api:convert": "mkdir -p tmp && sh scripts/convert_swagger_to_oapi3.sh",
    "console-api:split": "rm -r tmp/split && mkdir -p tmp/split && openapi split tmp/console_oapi3.json --outDir tmp/split",
    "console-api:integrate": "mkdir -p openapi/paths/ && cp tmp/split/components/schemas/*.yaml openapi/components/schemas/ && cp tmp/split/paths/*.yaml openapi/paths/",
    "console-api:clean": "git checkout openapi && git clean -fd openapi",
    "docs:generate": "redocly build-docs openapi-with-examples.json --title 'Edgio API Reference' --output 'dist/rest_api.html'",
    "docs:bundle": "yarn docs:generate && python3 scripts/css.py && python3 scripts/far.py && cp -r assets/* dist"
  },
  "devDependencies": {
    "@redocly/cli": "^1.2.0",
    "@redocly/openapi-cli": "^1.0.0-beta.95",
    "snippet-enricher-cli": "^0.0.8"
  }
}
