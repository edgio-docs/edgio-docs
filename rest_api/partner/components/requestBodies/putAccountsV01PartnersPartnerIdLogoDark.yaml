content:
  multipart/form-data:
    schema:
      type: object
      properties:
        logo:
          description: The partner's logo.
          type: string
          format: binary
      required:
        - logo
required: true
