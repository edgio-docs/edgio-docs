import {default as base} from './base.config';

const COMPANY_NAME = 'Edgio';
const PORTAL_CONTROL = 'Control Portal';
const PRODUCT_OPEN_EDGE = 'Open Edge';

const config = {
  COMPANY_NAME,
  PORTAL_CONTROL,
  PRODUCT_OPEN_EDGE,
};

export default config;
