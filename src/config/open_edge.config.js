import {default as base} from './base.config';

const COMPANY_NAME = 'Edgio';
const PORTAL_OPEN_EDGE = `${COMPANY_NAME} Control Portal`;

const config = {
  COMPANY_NAME,
  PORTAL_OPEN_EDGE,
};

export default config;
