import { VersionContext } from "components/VersionProvider";

const VersionedGuide = () => {
  // Get the version from the provider
  const version = VersionContext. 
  return <></>;
};

export default VersionedGuide;
